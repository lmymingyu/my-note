# misc

## sudo apt-get install xxx    安装软件

## sudo su 切换到管理员帐号   exit回到普通用户

# git:
	git clone git://github.com/happypeter/taiyuan-note.git
	git add 文件
	git commit -a -m "xxx"
	git throwh  回复
	git clone
	diff  查找两文件差异                  13700507175
		diff -u x x >x.diff 生成diff
	patch 补丁
		patch x <x.diff  打入补丁
	git config --global user.name "lmyming" git改名
		vim .gitconfig   同上
		patch x < x.diff -R 返回补丁前
	ssh-keygen -t dsa 创建钥匙
	sudo apt-get install xclip
	cat id_dsa.pub |xclip -sel clip
	git checkout 版本号   回到过去
	git branch 查看分支
	git checkout master 回到分支  （对应git checkout 版本号 ）

# note:
	markdown note.md >h.html
	firefox h.html
	#gconftool  --recursive-unset  /apps/panel
	rm  -rf  ~/.gconf/apps/panel
	pkill  gnome-panel #  面板恢复命令
	


